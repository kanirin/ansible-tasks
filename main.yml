- name: Install Apache
  yum:
    name: httpd
    state: latest

- name: Start service Apache
  systemd:
    name: httpd
    state: started
    enabled: yes

- name: Install epel-release
  yum:
    name: epel-release
    state: present